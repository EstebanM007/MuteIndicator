<Project Sdk="Microsoft.NET.Sdk">
  <PropertyGroup>
    <!-- Configuracion basica del proyecto -->
    <OutputType>WinExe</OutputType>
    <TargetFramework>net8.0-windows</TargetFramework>
    <UseWindowsForms>true</UseWindowsForms>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    
    <!-- Informacion de la aplicacion -->
    <AssemblyName>IconMute</AssemblyName>
    <RootNamespace>IconMute</RootNamespace>
    <Product>IconMute</Product>
    <Description>Aplicacion que muestra un indicador visual cuando el microfono esta silenciado</Description>
    <Authors>Developer</Authors>
    <Version>1.0.7</Version>
    <ApplicationIcon>BotonR.ico</ApplicationIcon>
    
    <!-- Optimizaciones para gaming -->
    <WarningLevel>4</WarningLevel>
    <TieredCompilation>true</TieredCompilation>
    <TieredCompilationQuickJit>true</TieredCompilationQuickJit>
    <ReadyToRun>true</ReadyToRun>
    <PublishReadyToRun>true</PublishReadyToRun>
    
    <!-- El usuario instala el runtime de .NET por separado -->
    <SelfContained>false</SelfContained>
    <PublishSingleFile>true</PublishSingleFile>
    <RuntimeIdentifier>win-x64</RuntimeIdentifier>
  </PropertyGroup>

  <ItemGroup>
    <!-- Dependencias de la aplicacion -->
    <PackageReference Include="NAudio" Version="2.2.1" />
  </ItemGroup>
</Project>
